[package]
name = "gpugeno"
version = "0.1.0"
edition = "2021"
description = "GPU-based BAM analysis prototype; currently the temporary bounded BGZF upload slice"

[dependencies]
libdeflater = "1.26.0"
